# AI Quant Trade - 里程碑路线图

> 基于项目现状锐评整理的关键改进路径，按优先级排序。

## 当前项目定位

**状态：** 原型到可用过渡中，核心链路已跑通

**最新进展（2026-07-19）：** AI 策略分析到自动下单闭环已完成。延迟从 3.4s 降至 175ms。OKX + DeepSeek API Key 已配置。待做：回测仓位管理、设置页 Key 写入后端。，但数据可靠性、状态持久化、健壮性三个环节离"可用"还有距离。

---

## 阶段一：让数据可信（P0 - 必须做）

### 1.1 Mock 数据开关 + 真实状态指示

- [x] 后端 API 返回 `_mock: true` 时，前端页面上显示橙色/黄色 Banner："当前使用模拟数据，交易所连接不可用"
- [x] 在导航栏/状态栏显示实时连接状态：OKX Testnet / OKX Live / 离线（模拟模式）
- [ ] 所有数据面板（仪表盘、交易、策略）在 mock 模式下明确标记数据源

**改动范围：** 前端 Layout 组件 + 各页面数据加载逻辑

### 1.2 交易所连接自动重试 + 错误上报

- [x] 后端 `ExchangeClient` 每次连接失败时记录详细错误原因（日志 + API 暴露）
- [ ] 指数退避重试机制
- [ ] 前端显示连接错误详情，而不是静默吞掉

**改动范围：** `backend/core/exchange.py` + 前端全局错误显示

### 1.3 运行时真实连接测试

- [x] 添加 `POST /api/exchange/test-connection` 端点（带超时和详细错误）
- [ ] 设置页面中可以一键测试 API 连接，显示延迟和错误信息

**改动范围：** `backend/api/market.py` + 前端设置页面

---

## 阶段二：状态不丢（P1 - 尽快做）

### 2.1 前端状态持久化

- [ ] 页面状态（选中的交易对、策略配置、视图偏好）存入 `localStorage`
- [ ] 页面刷新后从 `localStorage` 恢复，不用重新配置
- [x] 回测历史结果存入 JSON 文件，页面刷新后可以查看上次结果

**改动范围：** 前端各页面 `useEffect` + 后端 `db/models.py` + `api/backtest.py`

### 2.2 后端数据恢复

- [ ] 回测结果持久化到 SQLite（新的 `BacktestRecord` 表）
- [x] 添加 `GET /api/backtest/history` 和 `POST /api/backtest/history/clear` 端点
- [ ] 前端回测页面增加历史记录侧边栏

**改动范围：** `backend/db/models.py` + `backend/api/backtest.py` + 前端 `backtest.tsx`

### 2.3 网格策略状态持久化

- [ ] 网格策略的各级状态（当前价格、持仓、订单 ID）保存到数据库
- [ ] 后端重启后从数据库恢复网格状态
- [ ] 策略运行记录（开仓时间、当前盈亏）持久化

**改动范围：** `backend/strategies/grid.py` + `backend/db/models.py`

---

## 阶段三：后端健壮性（P1 - 重要）

### 3.1 全局错误处理中间件

- [ ] FastAPI 全局异常处理，统一错误返回格式
- [ ] 记录错误日志到文件（`logs/error.log`）
- [ ] 敏感信息（API Key、Secret）不出现在错误消息中

**改动范围：** `backend/main.py` + 新建 `backend/core/errors.py`

### 3.2 仓位管理 + 风险控制

- [ ] 回测引擎支持可配置的仓位管理（固定比例、凯利公式、波动率调整）
- [ ] 实盘下单前检查风控限制（最大持仓、每日亏损、单笔金额）
- [ ] 风控参数可在设置页面调整

**改动范围：** `backend/strategies/backtest.py` + `backend/core/risk.py` + 前端设置页面

### 3.3 回测异步化

- [ ] 长时间回测（5000+ 根 K 线）放到后台任务执行
- [ ] 添加任务状态端点 `GET /api/backtest/status/{task_id}`
- [ ] 前端轮询任务进度

**改动范围：** `backend/api/backtest.py` + 前端 `backtest.tsx`

---

## 阶段四：前端体验（P2 - 锦上添花）

### 4.0 AI 策略到自动下单闭环

- [x] AI 分析结果后显示下单面板
- [x] 市价单直接发到 OKX 测试网
- [x] 显示订单状态

### 4.1 页面切换不丢失 WebSocket 连接

- [ ] 如果未来用 WebSocket 推流，连接挂在顶层组件而非页面级别
- [ ] 切换页面时 WebSocket 不断开，数据不中断

### 4.2 错误 Toast 提示

- [x] 所有 API 调用失败时，前端显示 Toast 通知
- [ ] 网络错误、交易所错误、API Key 错误分别用不同颜色

### 4.3 数据加载骨架屏

- [ ] 各页面数据加载中显示骨架屏组件（目前只有简单的 "加载中..." 文本）

---

## 阶段五：测试覆盖（P2 - 长期）

### 5.1 后端单元测试

- [ ] `test_exchange.py` — 交易所客户端 mock 测试
- [ ] `test_backtest.py` — 回测引擎策略测试
- [ ] `test_api_*.py` — API 端点测试

### 5.2 前端测试

- [ ] 页面渲染测试（保证路由正常）
- [ ] API 调用集成测试

---

## 优先级矩阵

| 任务 | 影响 | 工作量 | 优先级 |
|------|------|--------|--------|
| Mock 状态指示 | 高 | 小 | P0 |
| 前端状态持久化 | 高 | 中 | P1 |
| 回测结果存 DB | 高 | 中 | P1 |
| 仓位管理 | 高 | 中 | P1 |
| 错误 Toast | 中 | 小 | P1 |
| 网格持久化 | 中 | 大 | P1 |
| 回测异步化 | 中 | 中 | P2 |
| WebSocket 连接 | 中 | 大 | P2 |
| 骨架屏 | 低 | 小 | P2 |
| 测试覆盖 | 高 | 大 | P2 |

---

_最后更新：2026-07-18_
