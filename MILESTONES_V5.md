# TradePilot V5 — 功能真实性修复

> **目标**：排查并修复所有「假数据/不可用/显示错误」的功能，确保所有功能可用、所有数据真实。
> **来源**：用户实测反馈的 7 项问题。

---

## 一、待办清单（用户反馈的 7 项）

### 1. 策略日志不显示心跳等内容
- [ ] 排查心跳/事件是否真正写入 `strategy_event_logs`（`log_event` 调用点 + 前端展示）

### 2. 仪表盘「最近成交」是假数据
- [ ] 排查 `/api/portfolio/trades` 数据源，确认无 mock 回退，只有真实 `Trade` 记录；空数据显示空态而非假数据

### 3. 策略加入策略库后胜率 1000%
- [ ] 排查胜率计算/存储/展示链路，定位 1000% 的 double-percent 或除零 bug

### 4. 回测应回测策略、而非自定义/模拟数据
- [ ] 排查回测数据源：`fetch_ohlcv` 交易所失败时回退 `_mock_ohlcv`（随机游走）→ 应返回错误而非假数据

### 5. 训练模型报 NaN
- [ ] `ml/features.py` 的 rolling 指标产生 NaN，`GradientBoostingClassifier` 不接受 NaN → 训练前 drop/impute NaN

### 6. AI 策略工厂每条策略都通不过过拟合检测
- [ ] 排查 `scientific_passed` 门槛是否过严（DSR>=0.5 + NWt>1.65），导致全部失败；校准为合理门槛

### 7. 全功能数据真实性排查
- [ ] 逐个排查所有 API 端点，确认无 mock/随机/硬编码数据，全为真实数据；不可用的返回明确错误

---

## 二、优先级

1. **3 胜率 bug**（显示错误，最直观）
2. **5 训练 NaN**（功能直接报错）
3. **2 + 4 假数据**（数据真实性）
4. **6 过拟合门槛**（统计校准）
5. **1 日志心跳**（功能缺失）
6. **7 全量排查**（收尾）

---

_本文档承接 MILESTONES_V4.md，聚焦「功能真实性」与「数据可用性」。_
